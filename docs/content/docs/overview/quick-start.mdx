---
title: Quick Start
description: Run Infinite Canvas in a few steps
---

# Quick Start

To try the project, deploy or start the `web/` frontend. Company Tencent Cloud VOD also needs `server/`.

## Deploy with Vercel

Import the repository into Vercel. The root `vercel.json` builds `web/`. Personal AI requests are sent directly from the browser. Company Tencent Cloud VOD is not available on this static deploy path.

## Run locally

```bash
git clone git@github.com:basketikun/infinite-canvas.git
cd infinite-canvas/web
bun install
bun run dev
```

Open `http://localhost:3000`. To enable the company Tencent Cloud VOD provider, copy `.env.example` to `.env`, fill the Tencent variables, and run `cd server && bun run dev` in another terminal.

## Run with Docker

```bash
docker build -t infinite-canvas .
docker run --rm -p 3000:3000 infinite-canvas
```

## First-time setup

- Open Settings and enter your `Base URL`, `API Key`, and model names.
- Open `/prompts` to fetch prompt repositories directly from the browser and cache them in IndexedDB.
- Configure WebDAV if you want to synchronize canvases, assets, and generation history across devices.

Canvas projects and My Assets are primarily stored in the browser. Personal provider API keys only last for the current browser session. Shared-provider secrets stay on the server; admins can reveal them in Settings and include them in a config export, while regular users only see a mask and export personal providers.
